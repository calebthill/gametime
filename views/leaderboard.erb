<!DOCTYPE html>
  <html lang="en">
    <head>
      <meta charset="utf-8">
      <title>Slacker News</title>
      <link rel="stylesheet" href="styles.css">
    </head>
    <body>
      <h1 class="title"> Leader Board </h1>
        <h4>
          <ol>
          <h3>Wins</h3>
          <% @win.each do |team, score| %>
          <li><%= "#{team} wins: #{score}" %></li>
          <% end %>
          <h3> Losses </h3>
          <% @lose.each do |team, score| %>
          <li><%= "#{team} losses: #{score}" %></li>
          <% end %>
          </ol>
        </h4>
    </body>
 </html>
